#!/bin/bash

a=10
b=20

add=$((a+b))
echo Addition of a and b are $add

sub=$((b-a))
echo Addition of a and b are $sub

mul=$((a*b))
echo Addition of a and b are $mul

div=$((b/a))
echo Addition of a and b are $div

mod=$((a%b))
echo Addition of a and b are $mod